# Durable Workflow Engine — environment variables
# No API keys required for the core engine.
# Copy to .env and fill in values when integrations are added.

# Optional: override the default database path
# WF_DB_PATH=~/.wf/runs.db
